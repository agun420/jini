name: Master Paid Alpaca Pipeline

on:
  workflow_dispatch:
  schedule:
    # NY market hours during daylight saving time.
    - cron: "30-59/5 13 * * 1-5"
    - cron: "*/5 14-19 * * 1-5"
    - cron: "10 21 * * 1-5"

permissions:
  contents: write

concurrency:
  group: master-paid-alpaca-pipeline
  cancel-in-progress: true

jobs:
  run-master-paid-alpaca-pipeline:
    runs-on: ubuntu-latest

    env:
      PYTHONPATH: src:.
      ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
      ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
      SEC_USER_AGENT: ${{ secrets.SEC_USER_AGENT }}
      ALPACA_DATA_FEED: "sip"
      ALPACA_MAX_SYMBOLS: "500"
      ALPACA_CHUNK_SIZE: "100"
      ALPACA_BAR_TIMEFRAME: "1Min"
      ALPACA_LOOKBACK_MINUTES: "390"
      ALPACA_RVOL_BASELINE_DAYS: "20"
      ALPACA_NEWS_ENABLED: "true"
      ALPACA_NEWS_LOOKBACK_HOURS: "24"
      ALPACA_NEWS_LIMIT: "50"
      PAPER_ORDER_SUBMISSION_ENABLED: "false"
      OUTCOME_LOOKBACK_DAYS: "7"
      ACCOUNT_TYPE: "unknown"
      MANUAL_APPROVAL_REQUIRED: "true"
      ENGINE_KILL_SWITCH: "false"
      DAILY_LOSS_CAP_DOLLARS: "150"
      MAX_REAL_TRADE_NOTIONAL: "250"
      MIN_PAPER_OUTCOMES_BEFORE_REAL: "50"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install package requirements if present
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install pytest

      - name: Run Alpaca Paid Market Scanner
        run: python scripts/run_alpaca_paid_market_scanner.py

      - name: Run SEC Catalyst Scanner
        run: |
          if [ -f scripts/run_sec_catalyst_scanner.py ]; then
            python scripts/run_sec_catalyst_scanner.py || echo "SEC catalyst layer failed safely; continuing."
          fi

      - name: Run FINRA Short Pressure Scanner
        run: |
          if [ -f scripts/run_finra_short_pressure_scanner.py ]; then
            python scripts/run_finra_short_pressure_scanner.py || echo "FINRA short-pressure layer failed safely; continuing."
          fi

      - name: Run Alpaca News Scanner
        run: |
          if [ -f scripts/run_alpaca_news_scanner.py ]; then
            python scripts/run_alpaca_news_scanner.py || echo "Alpaca news layer failed safely; continuing."
          fi

      - name: Run Advanced Signal Quality
        run: |
          if [ -f scripts/run_advanced_signal_quality.py ]; then
            python scripts/run_advanced_signal_quality.py || echo "Advanced quality layer failed safely; continuing."
          fi

      - name: Run Halt LULD Circuit Guard
        run: |
          if [ -f scripts/run_halt_luld_circuit_guard.py ]; then
            python scripts/run_halt_luld_circuit_guard.py || echo "Market guard layer failed safely; continuing."
          fi

      - name: Run Signal Journal
        run: |
          if [ -f scripts/run_signal_journal.py ]; then
            python scripts/run_signal_journal.py || echo "Signal journal failed safely; continuing."
          fi

      - name: Run Outcome Labeler
        run: |
          if [ -f scripts/run_outcome_labeler.py ]; then
            python scripts/run_outcome_labeler.py || echo "Outcome labeler failed safely; continuing."
          fi

      - name: Run Adaptive Guard
        run: |
          if [ -f scripts/run_adaptive_guard.py ]; then
            python scripts/run_adaptive_guard.py || echo "Adaptive guard failed safely; continuing."
          fi

      - name: Run Paper Execution Gate Plan
        run: |
          if [ -f scripts/run_paper_execution_gate.py ]; then
            python scripts/run_paper_execution_gate.py || echo "Paper execution gate plan failed safely; continuing."
          fi

      - name: Run Slippage Fill Tracker
        run: |
          if [ -f scripts/run_slippage_fill_tracker.py ]; then
            python scripts/run_slippage_fill_tracker.py || echo "Slippage tracker failed safely; continuing."
          fi

      - name: Run Real Money Readiness Guard
        run: |
          if [ -f scripts/run_real_money_readiness_guard.py ]; then
            python scripts/run_real_money_readiness_guard.py || echo "Real-money readiness guard failed safely; continuing."
          fi

      - name: Write Paid Pipeline Health
        run: |
          if [ -f scripts/write_paid_pipeline_health.py ]; then
            python scripts/write_paid_pipeline_health.py
          fi

      - name: Run Final Repo Audit
        run: |
          if [ -f scripts/run_final_repo_audit.py ]; then
            python scripts/run_final_repo_audit.py || echo "Final repo audit failed safely; continuing so data can be committed."
          fi

      - name: Commit paid Alpaca pipeline data
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add docs/data/prediction_engine state/prediction_engine
          git commit -m "Update paid Alpaca elite scanner pipeline data" || echo "No changes to commit"
          git push
